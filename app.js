//import "dotenv/config";
// import express from "express";
// // find out how to import tailwind and postcss
// import bodyParser from "body-parser";
// import ejs from "ejs";

// swagger
// routes

// logger.info('NODE_ENV: ', process.env.NODE_ENY);

// if (process.env.NODE_ENV === 'test') {
// logger.info(DEVELOPMENT ENVIRONMENT DETECTED, ROUTE AUTH DISBALED!');

//}

//const app = express();

// app.use(express.json({
//   verify(req, res, buf) {
//     req.rawBody = buf;
//   },
//   limit: '50mb'
// })
//)

// app.use(
//   express.text({
//     verfiy(req, res, buf) {
//       req.Rawbody = buf;
//     },
//     limit: '50mb'
//   })
// )

// ROUTES
// app.use('/swagger', swagger)
// app.use('v1/yahooFinance', yahooFinanceV1);)

module.exports = app;
